@model IEnumerable<CCMS.Models.HeadCategory>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HeadCategoryId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HeadCategoryName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HeadCategoryDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsHeadCategoryActive)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HeadCategoryId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HeadCategoryName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HeadCategoryDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsHeadCategoryActive)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
